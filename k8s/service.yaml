apiVersion: v1
kind: Service
metadata:
  name: springboot-webapplication
spec:
  type: NodePort
  selector:
    app: springboot-webapplication
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30080
